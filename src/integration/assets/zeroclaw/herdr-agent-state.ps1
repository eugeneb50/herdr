# installed by herdr
# managed by herdr; reinstalling or updating the integration overwrites this file.
# HERDR_INTEGRATION_ID=zeroclaw
# HERDR_INTEGRATION_VERSION=1

# ZeroClaw reports agent state via its internal hook (not via shell hooks).
# This file exists so herdr recognizes "zeroclaw" as a valid integration target.
# The actual state reporting happens through the Unix socket API.