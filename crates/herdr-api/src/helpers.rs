//! API helpers - stub

pub fn encode_api_text(_runtime: &(), _text: &str) -> Vec<u8> {
    Vec::new()
}

pub fn encode_api_keys(_runtime: &(), _keys: &[String]) -> Result<Vec<Vec<u8>>, String> {
    Ok(Vec::new())
}
